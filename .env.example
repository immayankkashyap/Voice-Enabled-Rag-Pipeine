# Required external services (never commit the populated .env file)
SARVAM_API_KEY=
GROQ_API_KEY=

# Optional for higher Hugging Face Hub rate limits or gated tokenizer models
HF_TOKEN=

# Runtime defaults; model and chunking parameters will be selected after profiling
SARVAM_LANGUAGE_CODE=en-IN
